<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- App base theme -->
    <!-- Compose Material3 does not ship a manifest theme; base off MaterialComponents and keep it minimal. -->
    <style name="Theme.Freezr" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:colorBackground">@android:color/background_light</item>
    </style>
    <style name="Theme.Freezr.NoActionBar" parent="Theme.Freezr"/>
</resources>
